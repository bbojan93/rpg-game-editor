<div class="attribute_<%= character_attribute.id %>">
  <%= image_tag(character_attribute.icon.url, size: "20x20") %>
  <%= character_attribute.attribute_name %>:
  <%= character_attribute.attribute_value %>
  <%= link_to 'Edit', new_character_attribute_path, remote: true %> |
  <%= link_to 'Delete', character_attribute_path(character_attribute), remote: true, method: :delete, data: {confirm: "Are you sure"} %>
</div>
