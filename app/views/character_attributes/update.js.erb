$("#updateattribute_<%= @character_attribute.id %>").modal('hide');

$(".attribute_<%= @character_attribute.id %>").fadeOut(500, function(){
  $(this).remove();
  $("#attributes").prepend("<%= j render @character_attribute %>");
});
